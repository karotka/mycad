// Stable public entry point for the 2D rendering subsystem. The implementation
// can now move independently without changing interaction-layer imports.
export { Canvas2DRenderer } from './Viewport';
